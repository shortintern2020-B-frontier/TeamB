import {
  describe, test, expect,
} from '../../test/test-utils';

// TODO: testを書く
describe('Signup', () => {
  test('check Signup behavior', () => {
    const msg = 'msg';
    expect(msg).toEqual(msg);
  });
});
